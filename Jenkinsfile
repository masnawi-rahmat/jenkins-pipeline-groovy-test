#!groovy

pipeline {
   agent any
   stages {
       stage('clearspace') {
           steps {
               println ("clearing space now")
               }
           }
       }
  }
                          
       /*stage('hello') {
           steps {
              println ("hello world")
              }
          }
      }*/
